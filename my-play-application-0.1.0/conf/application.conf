# https://www.playframework.com/documentation/latest/Configuration

# This is the main configuration file for the application.
# ~~~~~

# The name of the current service
# serviceName = ${?SERVICE_NAME}
# # URLs for other services
# serviceA.url = ${?SERVICEA_URL}
# serviceB.url = ${?SERVICEB_URL}
# Add other service URLs as needed


http.port = 9000
# The name of the current service
# serviceName = "yourServiceName"

# # URLs for other services
# serviceA.url = "http://serviceA-url"
# serviceB.url = "http://serviceB-url"
# Add other service URLs as needed


# serviceA.url = "http://localhost:9001/serviceA"
# serviceB.url = "http://localhost:9002/serviceB"
# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.http.secret.key = "i]QC:9Hdlk>cPAQv[s?hg[lhbR@Npi]5:YAlK=[X=zsqy1QO?zoesTAXpQY_fMU["

# Increasing the default value of 128K to allow processing to subOrderIds to checkoutIds required in logistics admin ui
play.http.parser.maxMemoryBuffer = 50M

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Custom Modules
# play.application.loader = util.configurations.app_loaders.MultienvConfigAppLoaderWithCircularProxies
#play.application.loader = modules.MMApplicationLoader

# play.modules.enabled += modules.BindModule

# play.modules.enabled += util.periodic_task_executor.PeriodicTaskExecutorModule

#From play2o6 the below filters are enabled by default
play.filters.disabled += play.filters.hosts.AllowedHostsFilter
# play.filters.disabled += "play.filters.csrf.CSRFFilter"

# Dev - Application Title for Kafka Consumer Group